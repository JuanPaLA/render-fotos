import React,{useEffect, useState} from 'react';
import '../index.css';

export default function Header(){
    return(
        <div id="header">
            Header + Cronómetro
        </div>
    )
}